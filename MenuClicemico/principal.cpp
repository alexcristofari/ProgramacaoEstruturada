#include <iostream>
#include <string>
#define TAM 100
using namespace std;

#include "metodos.h"

int main(){
    -Glicemia Listada[TAM];
    int totalCadastrados = 0;

    totalCadastrados = conectarComBase("")

    conectarComBase("glicemia.csv", lista TAM);

    menu(lista, totalCadastrados);
    totalCadastrados = menu(Lista, totalCadastrados, TAM);
    
    return 1;

}

